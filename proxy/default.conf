server {
    listen 8080;

    location /static {
        alias /vol/static;
    }

    location /favicon.ico {
        alias /vol/static/static/favicon.ico;
    }

    location / {
        uwsgi_pass app_1:8000;
        include /etc/nginx/uwsgi_params;
    }
}