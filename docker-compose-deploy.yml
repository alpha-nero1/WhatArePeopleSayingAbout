version: '3.3'

services: 
    app:
        build:
            context: .
        volumes:
            - static_data:/vol/web
        environment:
            - SECRET_KEY=samplesecretkey123
            - ALLOWED_HOSTS=*
            - GOOGLE_RECAPTCHA_SECRET_KEY=6Ld9m3AbAAAAAP_eSeALEvzuMNlvCgKBlI0HxyiV
            - ENDPOINT_URL=whatarepeoplesayingabout.com
            - PG_DB=postgres
            - PG_USER=postgres
            - PG_HOST=database-2.chfbgd0lcuju.ap-southeast-2.rds.amazonaws.com
            - PG_PASS=QuK5Erby4MfVZau3gUso
            - PG_PORT=5432

    proxy:
        build:
            context: ./proxy
        volumes:
            - static_data:/vol/static
        ports:
            - "80:80"
            - "443:443"
        depends_on:
            - app

# Create the shared volume
volumes:
    static_data: